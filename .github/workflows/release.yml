name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  quality:
    name: Quality checks
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.1', '8.2', '8.3', '8.4']

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Validate composer.json
        run: composer validate --strict

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-interaction

      - name: PHPStan
        run: composer phpstan

      - name: PHP CS Fixer
        run: composer cs-check

      - name: PHPUnit
        run: composer test

  release:
    name: Build & publish release
    needs: quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build plugin zip
        run: git archive --format=zip --prefix=health-check-wordpress/ -o health-check-wordpress.zip "${{ github.ref_name }}"

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: health-check-wordpress.zip
          name: ${{ github.ref_name }}
          body: |
            ## ${{ github.ref_name }}

            See the [changelog](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }}) for details.

            ### Installation
            Download the `health-check-wordpress.zip` asset below, then upload it in WordPress Admin → Plugins → Add New → Upload Plugin.
